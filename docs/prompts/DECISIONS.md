# Decisions

Every question waiting on Craig, in one place. An agent adds a row at each STOP (charter section 5) and never answers its own row. When Craig replies, the agent pastes the reply verbatim, sets `DECIDED`, and cites the id in the ledger row or handoff that depended on it. `npm run agents:check` rejects a row without a recommendation, a status other than OPEN or DECIDED, or DECIDED without a reply. **Blocks** names what cannot proceed while the row is OPEN; `entry <N>` is machine-read, so `npm run agents:check -- --entry <N>` fails until Craig answers.

Reply in chat with the ids, for example: `D-001 approved all. D-004 yes after 06. D-006 no.`

| Id | Raised | Question | Recommendation | Blocks | Status | Craig's reply (verbatim) | Date |
| --- | --- | --- | --- | --- | --- | --- | --- |
| D-001 | Prompt 01 STOPs 1.2 to 1.6 (2026-09-10) | The review halves of prompt 01 were never answered: the title and subtitle (STOP 1.2, P1-005), the full script (1.3, P1-006), the six story-surface screenshots (1.4, P1-007), the pause, options and game-over screenshots (1.5, P1-009) and the stage briefs (1.6, P1-012, "approved by proceeding"). Approve them? | Approve all; you moved on to plan v2 without objections. Name any screen you want redone and it goes into 05 or 08. | entry 5 | OPEN | | |
| D-002 | Art pass (2026-09-10) | EVAL-ART-001 (ten boss sheets) and ART-004 (HD text) passed their gates; do you approve the boss art and the HD text as they look now? | Approve; 07 revisits boss art quality anyway. | entry 5 | OPEN | | |
| D-003 | Handoff 01 status (2026-09-22) | The handoff header still held the template's "PARTIAL (list what is missing and why)" although commit `a1200f4` is "handoff COMPLETE" and its exit-gate evidence lists P1-001..012 PASS. The agent system corrected the header to COMPLETE so prompt 05's entry check can pass. Confirm prompt 01 is complete? | Confirm. | entry 5 | OPEN | | |
| D-004 | STOP 9.4 (2026-09-22) | Play Pyro Maw to the boss, pause twice, open Options twice (the HUD is now baked into textures, music loads on demand, menus are laid out in game pixels). Does anything look or sound different? | Expect no difference except menus now centred on a big window; if anything is off, say what and where. | 09 exit | OPEN | | |
| D-005 | 09c disk (2026-09-22) | `assets/sprites/source/` holds about 115MB of February source sheets that manifests still reference. After 06 regenerates every family through Higgsfield they are obsolete. Archive them outside the repo then? | Yes, after 06 closes: move to a release asset or external folder, point the manifests at the archive, keep git history as is. |  | OPEN | | |
| D-006 | 09c disk (2026-09-22) | `.git` holds 2,466 unreachable loose objects (73MB; 1,480 older than two weeks; 4 unreachable commits, probably old stashes). A normal `git gc` deletes the old ones permanently. Run it? | Look first with `git fsck --lost-found` (it writes the four commits to `.git/lost-found/`); if nothing there matters, run `git gc`. |  | OPEN | | |
| D-007 | 09c disk (2026-09-22) | `npm run clean:artifacts` found 45.6MB in `tmp/` and uncited `output/` files (debug images from March, many cut from ripped sheets). Move them to the Trash with `npm run clean:artifacts -- --yes`? | Yes; anything a doc cites is kept, and the Trash keeps the rest recoverable. |  | OPEN | | |
| D-008 | 09c disk (2026-09-22) | Docker Desktop on this Mac holds 46GB of images (38GB reclaimable) and 21GB of build cache, all from other projects (adinsights, pricing, airbyte), not this game. Clear the build cache with `docker builder prune`? | Yes for the build cache (it rebuilds on demand); leave images unless those projects are retired. |  | OPEN | | |
| D-009 | 09a review (2026-09-22) | The boss loop is 152s, about 58MB of decoded audio while it plays (budget 64MB). Shorten it or stream long tracks in prompt 08? | Prompt 08's Audio Director shortens loops to 60 to 90s with clean seams; streaming only if a track must stay long. |  | OPEN | | |
| D-010 | 09a (2026-09-22) | The render scale is capped at 6: on a window above 3x zoom on a 2x display the browser scales the canvas up, so text edges are resampled (game pixels stay exact). Keep the cap? | Keep; look at the Title on your largest monitor once. |  | OPEN | | |
| D-011 | Prompt 10 (2026-09-22) | Should `npm run agents:check` run in CI on every push (it is green now and already part of `npm run verify`)? | Yes: add it to `.github/workflows/ci.yml` beside test and build. | 10b | OPEN | | |
| D-012 | Prompt 10 (2026-09-22) | Which tool runs the blind review seats: Claude Code subagents (`.claude/agents/game-*.md`), Codex (`scripts/agents/run-seats.sh`), or both? The Codex CLI on this Mac is broken (its binary is missing; `npm i -g @openai/codex` reinstalls it). | Both: Claude subagents when Claude orchestrates, `run-seats.sh` when Codex does; the seat files are shared. |  | OPEN | | |

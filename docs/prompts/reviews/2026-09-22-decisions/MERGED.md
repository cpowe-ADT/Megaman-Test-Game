# Merged decisions: 2026-09-22-decisions

| Decision | Result | Verdicts | Conditions |
| --- | --- | --- | --- |
| D-001 | DECIDED | game-director APPROVE WITH CONDITIONS; level-designer APPROVE WITH CONDITIONS; narrative-designer APPROVE WITH CONDITIONS; narrative-designer APPROVE WITH CONDITIONS | game-director: (1) The full-width lane grows to 38px on 14 of 20 radio lines (4.5s each) and hides the floor row, the player's legs and floor-level enemies and flame jets; prompt 05 keeps floor hazards visible while radio plays (reposition the lane or make it translucent over the playfield), then reshoots 35. (2) Minor: credit lines faintly show through the footer band. / level-designer: (1) Gale's boss room: the brief says `flat` (line 125) but 06 §6.1 and 07 §7.4 make it the `shaft` room Craig asked for; change to `shaft`. (2) `rising_liquid` only rises once and `wind_zone` only pushes sideways, so Tide's cycling water and Ferro's vertical magnet lift do not exist yet: before 06e, 6.2 adds a cycle and direction to `rising_liquid` and a vertical axis to `wind_zone`, lab-tested, or the briefs drop those mechanics. (3) Q1, Tide's timed-water `sub_tank`: not as written (no swim, the lint checks fixed geometry); use a `carry` float riding the water line to the gate, linted from the float's highest point, else a `breakable_wall`. (4) Q2, Volt's `armor_legs` capsule: keep it on the main route (capsules are progression; only `heart_tank` and `sub_tank` count as secrets); lint every master segment with base movement and no capsule. (5) Q3, Ferro's `enemy_frost_turret`: yes as a palette variant (quench nozzle, steam, same two-shot pattern); add a Ferro row to the ecology plan so 6.4 generates it; optional. (6) Housekeeping: the header names prompt 02 instead of 06; the tutorial says 3 to 4 types but lists five; Glacier says six but lists seven; in 06 §6.5 `lane_vents` means the library's timed `vent`. / narrative-designer: 07 §7.6 also: (1) defeat lines true for any placed reward ("{rewardLabel} will carry you above its walls" is wrong for a Heart Tank or Access item); (2) rewrite Glacier's OMEGA radio line ("Memory is the last district"), which reads differently by play order; (3) extend A.6 to the tutorial ("drill hangar" lower case vs the "SENTINEL DRILL" card); (4) limit style-guide rule 15 to in-fiction lines so the "AI studio" credit stays. / narrative-designer: (1) Fix the ticker before `tutorial_coach` ships on it (prompt 05, not 08): wrap to two lines inside the lane, or cap ticker lines in the validator at what the lane shows; smoke 35 asserts the text stays in bounds. (2) 07c wires `miniboss_callout` and `finale_phase` (EVAL-P7-007) only after (1): the Core offer is 112 characters. (3) 07's STOP captures use the public build (the HUD reads "MEGA MAN X" above "Drill hangar, WREN.") and a mid-scroll credits frame (the credits capture is blank). |
| D-002 | DECIDED | art-director APPROVE WITH CONDITIONS | art-director: (1) Prompt 07 captures each boss's idle, move and shoot in the game at 1x, and this seat reviews them before any sheet counts as accepted. (2) 07 re-cuts glacier_ronin and ferro_blade with more space per cell and beams inside the cell. (3) `RETRY ×03` sits on the boss spawn point in 8 of 10 rooms: move it; Gale Vixen and Glacier Ronin are hard to see against their sky and snow backdrops. (4) 08 bundles one font: the HUD and Stage Select use generic `monospace` (Courier on macOS), the menus Trebuchet and Arial. |
| D-003 | DECIDED | qa-eval APPROVE WITH CONDITIONS | qa-eval: (1) Handoff line 508 says P1-001 to 012 all PASS: correct it (006, 007, 009 gate half only; 012 PENDING, D-001). (2) Delete the stale handoff lines 8 ("running slice memo") and 534 ("Not ready: prompt 01 exit conditions are incomplete"). (3) Fill Commit cells: P1-005 `6fa380e`, P1-006 `0e4ea1b`, P1-007 and P1-008 `12bbae3`. (4) Inputs for prompt 02 omit the last smoke number (38c), the `stageIntro` phase enum and the input action names; low impact since 02 is superseded. |
| D-004 | DECIDED | game-director APPROVE; qa-eval APPROVE WITH CONDITIONS | game-director: Pre-boss is not a like-for-like pair (the exit frame caught Pyro Maw's intro dialogue: state timeMs 2017 vs 1147): reshoot at the same moment. Sound not judged: no audio evidence; the state shows the same cue and loaded track. The radio overflow predates 09 (see the 09a shot). / qa-eval: (1) The fix waits 12 animation frames against a 160ms scene-time window, about 40ms of margin at 60Hz and less at 120Hz or when throttled: wait on state instead (retry the advance until `lineIndex === 1`, or until scene time passes the debounce). (2) Headroom is thin (Phaser gzip 294.2 of 300KB, Title audio 15.64 of 16MB, hiDpi exactly at its limit): re-measure before tightening further. (3) These gates cannot hear audio or judge menus on a big window; that half is the Director seat's or Craig's. `RETRY ×03` overlapping the boss floor predates 09. |
| D-005 | DECIDED | art-director APPROVE WITH CONDITIONS; release-engineer APPROVE WITH CONDITIONS | art-director: (1) Keep `boss_*_hf_v1_20260910*`, the prompt `.md` files and `free-source-attribution.v1.json` in the tree. (2) Archive only sheets 05 and 06 have replaced; the old boss sheets (about 36M) can go now. (3) Record the archive location and sha1 in the manifests; `npm run credits:build` still lists every CC-BY credit. (4) Craig runs the move to outside storage. / release-engineer: (1) Archive only superseded sheets; keep the 13 `*_hf_*` sheets, both manifests and `free-source-attribution.v1.json` (CC-BY). (2) Repoint or retire `sprites:coverage` in the same commit; `npm run verify` passes. (3) External folder; a GitHub release asset is publishing, so Craig chooses. (4) No history rewrite. |
| D-006 | DECIDED | release-engineer APPROVE WITH CONDITIONS | release-engineer: Tag first: `git tag archive/09b-effect-pooling 1c27293` (local, not pushed); then Craig runs `git gc` and `git count-objects -vH`. Never `--prune=now`. |
| D-007 | DECIDED | docs-steward APPROVE WITH CONDITIONS; release-engineer APPROVE WITH CONDITIONS | docs-steward: (1) Re-run the dry run just before `--yes` and proceed only if it lists the same 99 paths. (2) `output/decisions/D-010` is uncited and kept only by the three-day rule (lapses 2026-09-25): cite it in D-010 first. (3) `docs/prompts/09-footprint-and-performance.md:147` states a different rule from the script and from line 184; make 147 match 184. / release-engineer: Craig runs `npm run clean:artifacts -- --yes` only after a fresh dry run still shows the same paths (the 3-day window adds paths over time); emptying the Trash is Craig's. |
| D-008 | DECIDED | release-engineer APPROVE WITH CONDITIONS | release-engineer: Nothing to run today; if the cache grows, `docker builder prune`. Never prune images or volumes. |
| D-009 | DECIDED | audio-director APPROVE WITH CONDITIONS | audio-director: Rule for 08: (1) every looping cue and every boss stem is 30 to 60s, cut on a bar line, loop seam under 3dB; a boss's two stems share length and tempo; decoded cost is seconds x 0.384MB because decoding resamples to the 48kHz context (a lower sample rate saves disk, not memory). (2) The phase-two stem decodes after the stage track is evicted and starts at the playing stem's offset, keeping the worst case near 49MB. (3) Streaming only for a non-looping track over 60s (ending, credits), through Phaser's single AudioContext; never stream loops or stems (media elements do not loop gaplessly everywhere, cannot sync a second layer, and are invisible to `decodedAudioMB`). (4) Budgets stay (`boss.decodedAudioMB` 64, `stage.decodedAudioMB` 16); the STOP 8.1 loudness and loop table gains a decoded-MB column and a switch-peak row (outgoing plus incoming plus SFX, at most 64MB); new SFX and biome ambience count against `stage`; raising a ceiling needs a ledger row. (5) Trimmed CC0 tracks are credited as edits; `assets/audio/credits/README.md:69` still says music loads in `Preload` and is corrected. |
| D-010 | DECIDED | art-director APPROVE | none |
| D-011 | DECIDED | release-engineer APPROVE WITH CONDITIONS | release-engineer: `fetch-depth: 0` on `actions/checkout@v4` in `test-and-build`, `npm run agents:check` before `npm ci`; merge into main with merge commits, not squash or rebase. |
| D-012 | DECIDED | docs-steward APPROVE WITH CONDITIONS; release-engineer APPROVE WITH CONDITIONS | docs-steward: (1) The seats README names Claude subagents as the default until a `run-seats.sh` review merges green after Codex is reinstalled. (2) `run-seats.sh` hardcodes REVIEW_FORMAT, so decision panels are Claude-only until it takes a format argument; say so or add the argument. (3) It writes `.<seat>.log` into the tracked reviews folder: send logs to `output/`. (4) `DECISION_FORMAT.md` is untracked and unlinked: commit it and link it from the seats README. / release-engineer: Claude subagents are the only runner, including decision panels, until `npm i -g @openai/codex` works and one `run-seats.sh` run passes `agents:reviews`. |

All decision files valid.

Seat: audio-director
Commit: da50dcc
Decisions: D-009
Artifacts opened: `docs/prompts/seats/audio-director.md`, `docs/prompts/seats/DECISION_FORMAT.md`, `docs/prompts/DECISIONS.md`, `src/audio/musicLibrary.ts`, `src/audio/musicResidency.ts`, `src/audio/PlaceholderAudioService.ts`, `src/audio/MusicTrackLoader.ts`, `tests/perf-budget.json`, `scripts/perf/footprint.mjs`, `docs/prompts/08-audio-presentation-and-release.md`, `docs/prompts/09-footprint-and-performance.md`, `docs/prompts/handoff/09-footprint-and-performance.md`, `assets/audio/credits/README.md`, ffprobe of `assets/audio/music/*.ogg`

| Decision | Verdict | Reason | Evidence | Conditions |
| --- | --- | --- | --- | --- |
| D-009 | APPROVE WITH CONDITIONS | Shorten, do not stream and do not leave as is. The boss room already measures 61MB of a 64MB budget and 8.1 adds a second boss stem: two 152s stems would hold about 117MB, two 90s stems still 69MB, so the 60 to 90s recommendation narrows to 60s at most. | `ffprobe` -> boss_loop 152.0s, stage_loop 32.0s, stage_select 33.4s, title_menu 11.1s, all 44.1kHz stereo; `docs/prompts/handoff/09-footprint-and-performance.md:32`; `tests/perf-budget.json:12`; `docs/prompts/08-audio-presentation-and-release.md:32`; `src/audio/musicResidency.ts:4` | Rule for 08: (1) every looping cue and every boss stem is 30 to 60s, cut on a bar line, loop seam under 3dB; a boss's two stems share length and tempo; decoded cost is seconds x 0.384MB because decoding resamples to the 48kHz context (a lower sample rate saves disk, not memory). (2) The phase-two stem decodes after the stage track is evicted and starts at the playing stem's offset, keeping the worst case near 49MB. (3) Streaming only for a non-looping track over 60s (ending, credits), through Phaser's single AudioContext; never stream loops or stems (media elements do not loop gaplessly everywhere, cannot sync a second layer, and are invisible to `decodedAudioMB`). (4) Budgets stay (`boss.decodedAudioMB` 64, `stage.decodedAudioMB` 16); the STOP 8.1 loudness and loop table gains a decoded-MB column and a switch-peak row (outgoing plus incoming plus SFX, at most 64MB); new SFX and biome ambience count against `stage`; raising a ceiling needs a ledger row. (5) Trimmed CC0 tracks are credited as edits; `assets/audio/credits/README.md:69` still says music loads in `Preload` and is corrected. |

Seat: docs-steward
Commit: da50dcc
Decisions: D-007, D-012
Artifacts opened: `docs/prompts/seats/docs-steward.md`, `docs/prompts/seats/DECISION_FORMAT.md`, `docs/prompts/seats/README.md`, `docs/prompts/DECISIONS.md`, `scripts/perf/clean-artifacts.mjs`, `docs/prompts/09-footprint-and-performance.md`, `docs/prompts/10-agent-system.md`, `assets/sprites/source/free-source-attribution.v1.json`, `.claude/agents/game-docs-steward.md`, `.claude/agents/game-code-reviewer.md`, `scripts/agents/run-seats.sh`, `output/probe-cams.mjs`, `docs/prompts/reviews/2026-09-22-10a/`

| Decision | Verdict | Reason | Evidence | Conditions |
| --- | --- | --- | --- | --- |
| D-007 | APPROVE WITH CONDITIONS | The dry run lists 99 paths, 45.6MB, matching the row. Every name and stem was grepped across 157 Markdown files (tracked, untracked, and ignored under `output/`, covering both ledgers, the handoffs and `docs/archive/progress/`): no listed path is cited; every hit named a different file. The CC0 downloads in `tmp/` already have copies in `assets/sprites/source/` with URL and licence recorded. | `scripts/perf/clean-artifacts.mjs:45`, `assets/sprites/source/free-source-attribution.v1.json:8` | (1) Re-run the dry run just before `--yes` and proceed only if it lists the same 99 paths. (2) `output/decisions/D-010` is uncited and kept only by the three-day rule (lapses 2026-09-25): cite it in D-010 first. (3) `docs/prompts/09-footprint-and-performance.md:147` states a different rule from the script and from line 184; make 147 match 184. |
| D-012 | APPROVE WITH CONDITIONS | Both. One source of truth: each `game-*.md` agent points at `seats/<seat>.md` plus `REVIEW_FORMAT.md`, and `run-seats.sh` builds the same brief; both write `reviews/<date>-<slice>/<seat>.md` and merge through `agents:reviews`, so logs match and the 600-word cap is shared. Only the Claude path is proven (the 10a reviews); `codex --version` fails with ENOENT today. | `.claude/agents/game-docs-steward.md:7`, `scripts/agents/run-seats.sh:26`, `docs/prompts/seats/README.md:29` | (1) The seats README names Claude subagents as the default until a `run-seats.sh` review merges green after Codex is reinstalled. (2) `run-seats.sh` hardcodes REVIEW_FORMAT, so decision panels are Claude-only until it takes a format argument; say so or add the argument. (3) It writes `.<seat>.log` into the tracked reviews folder: send logs to `output/`. (4) `DECISION_FORMAT.md` is untracked and unlinked: commit it and link it from the seats README. |

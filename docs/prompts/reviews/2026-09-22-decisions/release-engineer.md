Seat: release-engineer
Commit: da50dcc
Decisions: D-005, D-006, D-007, D-008, D-011, D-012
Artifacts opened: `docs/prompts/seats/release-engineer.md`, `docs/prompts/seats/DECISION_FORMAT.md`, `docs/prompts/DECISIONS.md`, `.github/workflows/ci.yml`, `vite.config.ts`, `scripts/agents/check.mjs`, `scripts/agents/checks.mjs`, `scripts/agents/run-seats.sh`, `.claude/agents/game-*.md`, `scripts/perf/clean-artifacts.mjs`, `scripts/sprites/check-coverage.mjs`, `scripts/credits/build-credits.mjs`, `assets/sprites/source/source-images.manifest.json`, `docs/prompts/handoff/09-footprint-and-performance.md`, `output/perf/footprint-09b-pools.md`

| Decision | Verdict | Reason | Evidence | Conditions |
| --- | --- | --- | --- | --- |
| D-005 | APPROVE WITH CONDITIONS | No release risk: `dist` already excludes the folder; only the working tree and CI checkout shrink (history keeps the 105.9MB pack). The folder also holds the current 2026-09-10 Higgsfield boss sheets. | `vite.config.ts:40`, `scripts/sprites/check-coverage.mjs:16`, `scripts/credits/build-credits.mjs:11` | (1) Archive only superseded sheets; keep the 13 `*_hf_*` sheets, both manifests and `free-source-attribution.v1.json` (CC-BY). (2) Repoint or retire `sprites:coverage` in the same commit; `npm run verify` passes. (3) External folder; a GitHub release asset is publishing, so Craig chooses. (4) No history rewrite. |
| D-006 | APPROVE WITH CONDITIONS | Five unreachable commits: `0d019e9` (pre-amend `cc3b91c`), stash `3583ab7`/`c974004` (shipped as `ca9f149`), stash `1c27293`/`7af93aa` (today; the only copy of the dropped effect-pooling code). A default `git gc` prunes none of these yet (under 14 days) and removes 1,481 older loose objects. `--lost-found` creates no refs. | `git show --stat 1c27293` -> VfxSfxRouter +219; `output/perf/footprint-09b-pools.md` | Tag first: `git tag archive/09b-effect-pooling 1c27293` (local, not pushed); then Craig runs `git gc` and `git count-objects -vH`. Never `--prune=now`. |
| D-007 | APPROVE WITH CONDITIONS | The dry run lists 99 paths, 45.6MB; nothing tracked or untracked cites any by path or basename; all gitignored, none a build input; the Trash keeps them recoverable. | `node scripts/perf/clean-artifacts.mjs` -> 99 paths, 45.6MB; `docs/content/assets.md:47` | Craig runs `npm run clean:artifacts -- --yes` only after a fresh dry run still shows the same paths (the 3-day window adds paths over time); emptying the Trash is Craig's. |
| D-008 | APPROVE WITH CONDITIONS | Safe but moot: no Dockerfile here; the build cache is 0B; 15 images (8.05GB) are active and belong to other projects; the row's 46GB/21GB figures are stale. | `docker system df` -> Build Cache 0B; `docker buildx du` -> Total 0B | Nothing to run today; if the cache grows, `docker builder prune`. Never prune images or volumes. |
| D-011 | APPROVE WITH CONDITIONS | Fast (0.5s) and green on a full clone, but a depth-1 checkout gives 17 false errors (ledger commits are not ancestors). | `scripts/agents/check.mjs:35`; depth-1 clone -> `PASS cites df30ca8, which git does not know` | `fetch-depth: 0` on `actions/checkout@v4` in `test-and-build`, `npm run agents:check` before `npm ci`; merge into main with merge commits, not squash or rebase. |
| D-012 | APPROVE WITH CONDITIONS | Both: the 10 `game-*` agents point at `docs/prompts/seats/<seat>.md`. Codex is broken today (`codex --version` -> ENOENT) and `run-seats.sh` has never run. | `scripts/agents/run-seats.sh:7` | Claude subagents are the only runner, including decision panels, until `npm i -g @openai/codex` works and one `run-seats.sh` run passes `agents:reviews`. |

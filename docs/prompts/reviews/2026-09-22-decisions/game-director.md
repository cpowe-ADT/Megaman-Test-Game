Seat: game-director
Commit: da50dcc
Decisions: D-001 (presentation half: STOPs 1.2, 1.4, 1.5), D-004
Artifacts opened: `output/phase-9-exit/final-smoke/` 4-title-controls (4 shots), 34-prologue-flow (4), 35-radio-ticker (iona, omega), 36-ending-flow (4), 38b shot-paused, 38 shot-options-changed and shot-options-revisited, 40-hd-render menu-title/options/stage-select-2x; `output/phase-9a/visual-sweep/pyro_maw/` and `output/phase-9-exit/final-sweep/pyro_maw/`; `output/perf/hud-compare-pyro_maw.png`; `output/perf/hd-check/title-2x.png`, `options-2x.png`; `output/phase-9a/full-smoke/35-radio-ticker/shot-radio-iona.png`; state JSONs for 35, 36 and the sweep; `src/ui/ToastLane.ts`; pixel diffs of 12 matching smoke shots from 09a and the 09 exit

| Decision | Verdict | Reason | Evidence | Conditions |
| --- | --- | --- | --- | --- |
| D-001 | REJECT | Radio lines run out of their panel: the panel ends near x=336, the Iona line runs to the screen edge (x=448) so "tonight would be." is never shown, and the OMEGA line runs about 35px past the border and under "RETRY ×02" (x 362 to 430). A player loses story text mid-stage. Every other screen reads in two seconds with clear hierarchy (title and subtitle, control map, stage select, prologue, briefing, stage card, pause, options). | `output/phase-9-exit/final-smoke/35-radio-ticker/shot-radio-iona.png`, `output/phase-9-exit/final-smoke/35-radio-ticker/shot-radio-omega.png`, `src/ui/ToastLane.ts:44` | To approve: wrap or split radio lines inside the lane, then reshoot 35. Also: `36-ending-flow/shot-credits.png` is blank because the credits start below the screen (`src/scenes/EndingScene.ts:177`), so credits went unreviewed; its footer reads "ENTER FINISH ESC FINISH"; the large empty panel in `shot-card-1.png` reads as missing art; the HUD says "MEGA MAN X" (dev skin) while the briefing says WREN. |
| D-004 | APPROVE | Nothing a player sees changed except menu centring: nine 1x smoke shots are pixel-identical between 09a and the 09 exit (title, controls, stage select, HUD, both options screens, briefing, ending card, record); the pause panel is identical (only the dimmed world differs); HUD rows match within 1.2/255 after removing the fade; sweep start identical, mid differs by one enemy and one flame frame, boss-room by fight state; the 2x title was off to the right and is now centred. | `output/perf/hud-compare-pyro_maw.png`, `output/perf/hd-check/title-2x.png`, `output/phase-9-exit/final-smoke/40-hd-render/menu-title-2x.png`, `output/phase-9-exit/final-sweep/pyro_maw/start.png` | Pre-boss is not a like-for-like pair (the exit frame caught Pyro Maw's intro dialogue: state timeMs 2017 vs 1147): reshoot at the same moment. Sound not judged: no audio evidence; the state shows the same cue and loaded track. The radio overflow predates 09 (see the 09a shot). |

Seat: principal-engineer
Commit: ffda356
Scope: 05b 5.7 tutorial (EVAL-P5-009): room locks, gates, camera hold and shaft ceiling, the wall type, tutorial_coach, smoke 49, automation payload
Artifacts opened: output/packets/05b-5.7-tutorial-principal-engineer.md; src/scenes/Game.ts (423-444, 798-811, 1322-1334); src/scenes/game/CameraDirector.ts (172-190); src/scenes/game/GameDebugHooks.ts:195-215; src/config/gameplayLayout.ts; src/main.ts; src/physics/PlatformCollisionSystem.ts
Tokens: 79972

| Severity | Finding | Evidence | Fix |
| --- | --- | --- | --- |
| MAJOR | When the hero leaves a held room, `restoreCamera` calls `applyStageCameraBounds`, which resets the camera to stage bounds and sets `bossRoomCameraLocked = false`. Failure case: on the tutorial, `crossBossGate` is called from room 0 (the lock armed at spawn, so `cameraRoom` is 0). Next frame `update` runs `activateBossEncounter` then `applyBossRoomCameraLock`; the adapter's POST_UPDATE in the same frame undoes the lock and `bossEncounterActive` stops it being applied again. Smoke 5 and the sweep would then shoot the tutorial boss room with the camera unlocked. Normal play is unaffected because the restore happens at x 2240. | `src/mechanics/adapters/RoomLockAdapter.ts:157-159`, `src/scenes/game/CameraDirector.ts:149`, `src/scenes/Game.ts:809-810`, `src/scenes/game/GameDebugHooks.ts:203` | Pass a restoreCamera that re-applies the boss lock when `bossRoomCameraLocked`, else the stage bounds. |
| MAJOR | The shaft ceiling (-244) drops back to 90 (`GAMEPLAY_ACTOR_CEILING`) in the first frame the hero's x leaves 896..1344, whatever its height. Failure case: the hero wall-kicks up the closed exit gate (a static collider, so the side is blocked and the kick counts as `wall_jump`, which opens the gate), drifts right past 1344 at y about -100, and Arcade world-bounds collision snaps the body to y 90 in one frame while the camera bounds jump to 0..252. Same when leaving left over x 896. | `src/mechanics/adapters/RoomLockAdapter.ts:155-158`, `src/mechanics/roomLock.ts:73-83`, `src/config/gameplayLayout.ts:4` | Keep the raised ceiling until the hero is below `base.y` or grounded, then restore it. |
| MAJOR | Smoke 49 warps into the channel between the walls (1080, 1100) and out past the gate (1400), so nothing proves a player can get over the left wall, climb the channel, clear the right wall (top y 50) and exit. A shaft that traps the player, or one they can walk past, still passes. The camera's vertical scroll is not asserted (only `cameraHeld` and `room.height`). The 3-attempt retry loop hides flaky kicks. | `scripts/smoke/tutorial-verbs.mjs:106,110-123`, `src/content/campaign.ts:651-652` | Replay from x 900 to past x 1344 with no warp; assert camera scrollY under 0 near the top; one attempt only. |
| MINOR | Saber hits are counted by sampling `slashPhase` once per rendered frame for an edge into `active`. At a low frame rate a short active window is missed, and any window wholly inside a physics pause is missed because the adapter returns early while paused; the scrap gate stays short of three hits. | `src/mechanics/roomLock.ts:111`, `src/mechanics/adapters/RoomLockAdapter.ts:92,127` | Expose a `lastSlashStartedAtMs` or a swing counter and detect its change. |
| MINOR | Gameplay reads the runtime's debug surface every frame while a lock is closed: a full debug object allocated per frame, and string keys (`lastDashStartedAtMs`, `lastProjectileSpawnMs`, `lastProjectile.chargeLevel`) that a rename silently breaks. Upward hurt knockback from the ground would count as `jump` if the adapter is reused in 06. | `src/mechanics/adapters/RoomLockAdapter.ts:110-129`, `src/mechanics/roomLock.ts:102` | Add a typed `getVerbSample()` to the runtime; skip the takeoff edge during hurt lock. |
| MINOR | Coach lines are picked by lock index (`lines[index]`) but the validator allows 4 to 6 lines for 5 locks: cutting to 4 silently drops step five; a 6th line never plays. | `src/scenes/game/StoryDirector.ts` (`onRoomLockArmed`), `src/content/dialogue/types.ts:69` | Validate the line count against the tutorial's `roomLocks.length`. |
| MINOR | The payload hardcodes `'roomLocks'` instead of `ROOM_LOCK_DATA_KEY`. | `src/main.ts:374` | Import the constant. |

Checked and sound: `buildStage` runs once per `create` (`src/scenes/Game.ts:1329`) after the player is created (`:1311`), so the colliders stay valid; a respawn in place keeps each lock's state; `load_game` (`:2119`), Quit and re-entry rebuild the locks fresh through SHUTDOWN and `destroy`, no duplicates; the 90 ceiling seals the one-screen gates; dialogue only adds lane items (no reward, flag or scene change); every smoke 49 lock is replayed with a wrong input first, so a no-op replay fails; TESTING.md is updated in the same commit; `Game.ts` is one line shorter. Not checked: the runtime's `getDebugState` field names, how hit-stop pauses the game, how the save snapshot records position, the platform rectangle origin; no gates run.

| Rubric | Score |
| --- | --- |
| Correctness: no path produces a wrong result or crash | 3 |
| Tests: each new branch has a failing-first test or smoke assertion | 3 |
| Contracts: automation hooks, save format and scene flow unchanged or updated with docs | 4 |
| Size: Game.ts shrinks or holds; logic in typed modules | 5 |

Verdict: FIX (the boss camera unlock after `crossBossGate` and the ceiling snap on leaving the shaft need fixing, and smoke 49 needs a shaft replay with no warps)

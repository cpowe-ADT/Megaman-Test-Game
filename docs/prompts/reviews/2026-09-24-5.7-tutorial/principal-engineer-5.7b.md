Seat: principal-engineer
Commit: bf3b51a
Scope: 05b 5.7b tutorial fix (EVAL-P5-009) re-review: are the first-pass findings closed, plus a regression hunt in the lane, ceiling, verb sample, boss restore, validator and smoke 49
Artifacts opened: docs/prompts/reviews/2026-09-24-5.7-tutorial/principal-engineer.md; output/packets/05b-5.7b-tutorial-fix-principal-engineer.md; src/mechanics/adapters/RoomLockAdapter.ts; src/mechanics/roomLock.ts:40-85; src/ui/ToastLane.ts; src/player/NewPlayerRuntime.ts:136-170; src/scenes/game/CameraDirector.ts:144-165; src/scenes/Game.ts:428-440,1140-1152,1204-1214; src/scenes/game/StoryDirector.ts:112-126; scripts/smoke/story-surfaces.mjs:100-175
Tokens: 77992

| Severity | Finding | Evidence | Fix |
| --- | --- | --- | --- |
| MAJOR | The new `tutorial_dash_exit` checkpoint is index 1, and `onCheckpoint` plays `${stageId}_radio` at `index === 1` when a checkpoint has no `radioSequenceId`. So the Iona and OMEGA radio pair now starts at x 912, 16px into the shaft and right behind Rook's step three, instead of at the shaft exit. When the hero reaches x 1344, `supersede` puts step four ahead of them; "Checkpoint 2" and the radio then play after the charge prompt, a room behind the hero. The shaft exit toast also becomes "Checkpoint 3". | `src/scenes/game/StoryDirector.ts:116`, `src/content/campaign.ts` (the 4-arg `checkpoint(...)` at the dash exit), `src/ui/ToastLane.ts:87` | Put `radioSequenceId` on `tutorial_shaft_exit` and stop the index-1 default from firing at the dash exit. Assert in smoke 49 which checkpoint queues the radio. |
| MINOR | If the hero clears the right wall and drifts past x 1344 above y 0 (the gate opens on the first kick), `room` is the locked charge room; the camera snaps from the shaft scroll to 0..252 and the hero is off screen until it falls. The raised-top camera only covers open rooms. | `src/mechanics/adapters/RoomLockAdapter.ts:152-158` | While `top < base.y`, use `Math.min(room.y, top - 8)` as the camera top in the room branch too. |
| MINOR | A swing is counted when it enters `startup`; a swing cancelled in startup by a hit still counts as a scrap-wall hit, and reach is checked at startup rather than when the swing is active. | `src/player/NewPlayerRuntime.ts:168`, `src/mechanics/adapters/RoomLockAdapter.ts:117` | Count on entry to `active`; the counter still catches edges between samples. |
| MINOR | `coachWhileArmed` waits up to 15s without doing the verb, so `phase === 'locked'` is true by construction; the old enqueue-only lane takes about 6.9s per lock and would pass too. `supersede` has no unit test. | `scripts/smoke/tutorial-verbs.mjs:46-47` | Arm lock N while Rook's line N-1 is showing, then assert hint N is showing within 3 frames; unit-test `supersede`. |
| MINOR | The coach line is found by `requiredInput`, not by lock: a sixth lock that repeats a verb replays line one while the validator passes; a seventh lock can never pass both the 4-to-6 line rule and the one-line-per-lock rule. | `src/scenes/game/StoryDirector.ts` (`onRoomLockArmed`, `lines.find`), `src/content/dialogue/validateDialogueContent.ts:258-266` | Pass the lock index to `onArmed` and pick the line by position, which the validator already enforces; derive the line bound from the lock count. |
| MINOR | Still open from the first pass: the `'roomLocks'` string literal. | `src/main.ts:374` | Import `ROOM_LOCK_DATA_KEY`. |

First-pass findings: MAJOR 1 (boss camera) closed: `src/scenes/Game.ts:479` re-applies the boss lock when `bossRoomCameraLocked` is set and POST_UPDATE runs after `update` sets it; the flag resets at `:477`, `:1149` and `:1211`; no restore runs with a stale flag. MAJOR 2 (ceiling snap) closed: `resolveWorldCeiling` has a unit test; respawn at x 928 lands inside the shaft room (896 to 1344) so raising the ceiling there is correct; in a one-screen room the raised ceiling lasts only while the hero is above y 90 and airborne; knockback cannot raise it outside the tall room; leaving the stage runs SHUTDOWN which destroys the adapter. MAJOR 3 (shaft smoke) closed: one replay from x 900 with no warp asserts the peak, scrollY below -100 and x past 1344; a no-op replay fails all three. MINORs 4 to 6 closed: the runtime counts swings; `getVerbSample` is typed and builds one small object per frame only while a lock is closed; takeoff during hurt lock is skipped (a wall kick during hurt lock still counts, correct if the motor allows it); the validator reads the campaign each run and a new lock without a matching coach line fails validation. Regression hunt: `supersede` removes only `tutorial_coach` items because other enqueues carry no channel; smoke 35 runs on `pyro_maw`, which never calls `supersede`. Not checked: no gates; the checkpoint helper's fields; whether the motor allows kicks during hitstun; the air-dash range.

| Rubric | Score |
| --- | --- |
| Correctness: no path produces a wrong result or crash | 3 |
| Tests: each new branch has a failing-first test or smoke assertion | 3 |
| Contracts: automation hooks, save format and scene flow unchanged or updated with docs | 4 |
| Size: Game.ts shrinks or holds; logic in typed modules | 5 |

Verdict: FIX (the three first-pass MAJORs are closed, but the new dash-exit checkpoint moved the tutorial radio into the shaft; a small content fix and a smoke assertion)

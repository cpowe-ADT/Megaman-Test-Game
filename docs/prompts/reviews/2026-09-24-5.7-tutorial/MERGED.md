# Merged review: 2026-09-24-5.7-tutorial

Seats: level-designer (SHIP, mean 3.5), level-designer (FIX, mean 1.8), narrative-designer (SHIP, mean 4.3), principal-engineer (FIX, mean 3.8), principal-engineer (FIX, mean 3.8)

| Severity | Seat | Finding | Evidence | Fix | Agreed |
| --- | --- | --- | --- | --- | --- |
| BLOCK | level-designer | Rook's tutorial_coach dialogue never advances past the dash line: shot-wall-kick.png (two wall faces visible, no dash ledges) and shot-armed-charge.png (armored bot and drone visible, well into segment 4) both show the identical caption "Step two. Burst your thrusters and cover the floor faster than your legs can. The gate listens for the burst." Segments 3 (wall kick) and 4 (charge) never get their own signpost, against the "each segment ... a radio signpost line" promise (docs/design/stage-briefs.md:22). | `output/smoke-runs/2026-09-24T16-33-00-659Z/49-tutorial-verbs/shot-wall-kick.png`, `shot-armed-charge.png` | Re-key onArmed's coach line to the armed room's requiredInput, not a leftover or cached string; assert the lane text per lock in smoke 49. |  |
| BLOCK | level-designer | The dash gap does not require a dash. Ledge a ends at x=656, ledge b starts at x=800 (`src/content/campaign.ts:649-650`), a 144px gap. A plain running jump (220px/s, impulse -400, gravity 1050) has air time 2 x (400/1050) = 0.762s and covers about 168px, so it clears the gap without dashing. Only the room-lock's verb check at the closed gate forces a dash tap, unrelated to the platforming. | `src/content/campaign.ts:649-650` (ledge x and width) | Widen the gap past about 170px, or lower a ledge so a plain jump falls short. |  |
| MAJOR | level-designer | Missing mid-route checkpoint. The brief puts checkpoint 2 right after the dash segment (`docs/design/stage-briefs.md:509`), but the built checkpoints are x44, x1392 (after the shaft) and x2450 (`src/content/campaign.ts:635-639`). Dying anywhere in jump, dash or the shaft (x0-1344) resets to x44: three teach segments and about 1300px lost. | `src/content/campaign.ts:635-639`; `docs/design/stage-briefs.md:509` | Add a checkpoint near x896-960 (end of the dash room). |  |
| MAJOR | level-designer | Enemy roster and secret from the brief are missing. The brief promises 8 placements across gunner_bot (2), shock_hopper (2), drone (2), shield_drone (1), rocket_bot (1) (`docs/design/stage-briefs.md:24,506`); built `enemyMarkers` only has shield_drone, rocket_bot and the charge lock's armored_bot (`src/content/campaign.ts:657-670`). The secret capsule plus hp_refill_large behind the saber wall and the last-screen crumble_group (`stage-briefs.md:22,505`) are absent: `StageExtensionPatch` has no `secrets` field (`campaign.ts:602-610`) and nothing is typed `crumble_group`. | `src/content/campaign.ts:602-610,657-670`; `docs/design/stage-briefs.md:22,24,505,506` | Add the missing enemy markers, a secrets field or room, and a crumble_group patch. (Orchestrator note: prompt 05 §5.7 orders the minimum machinery; the roster, the secret and the crumble group are prompt 06 §6.1 and §6.2 work and are deferred there, recorded in the ledger row.) |  |
| MAJOR | principal-engineer | The new `tutorial_dash_exit` checkpoint is index 1, and `onCheckpoint` plays `${stageId}_radio` at `index === 1` when a checkpoint has no `radioSequenceId`. So the Iona and OMEGA radio pair now starts at x 912, 16px into the shaft and right behind Rook's step three, instead of at the shaft exit. When the hero reaches x 1344, `supersede` puts step four ahead of them; "Checkpoint 2" and the radio then play after the charge prompt, a room behind the hero. The shaft exit toast also becomes "Checkpoint 3". | `src/scenes/game/StoryDirector.ts:116`, `src/content/campaign.ts` (the 4-arg `checkpoint(...)` at the dash exit), `src/ui/ToastLane.ts:87` | Put `radioSequenceId` on `tutorial_shaft_exit` and stop the index-1 default from firing at the dash exit. Assert in smoke 49 which checkpoint queues the radio. |  |
| MAJOR | principal-engineer | When the hero leaves a held room, `restoreCamera` calls `applyStageCameraBounds`, which resets the camera to stage bounds and sets `bossRoomCameraLocked = false`. Failure case: on the tutorial, `crossBossGate` is called from room 0 (the lock armed at spawn, so `cameraRoom` is 0). Next frame `update` runs `activateBossEncounter` then `applyBossRoomCameraLock`; the adapter's POST_UPDATE in the same frame undoes the lock and `bossEncounterActive` stops it being applied again. Smoke 5 and the sweep would then shoot the tutorial boss room with the camera unlocked. Normal play is unaffected because the restore happens at x 2240. | `src/mechanics/adapters/RoomLockAdapter.ts:157-159`, `src/scenes/game/CameraDirector.ts:149`, `src/scenes/Game.ts:809-810`, `src/scenes/game/GameDebugHooks.ts:203` | Pass a restoreCamera that re-applies the boss lock when `bossRoomCameraLocked`, else the stage bounds. |  |
| MAJOR | principal-engineer | The shaft ceiling (-244) drops back to 90 (`GAMEPLAY_ACTOR_CEILING`) in the first frame the hero's x leaves 896..1344, whatever its height. Failure case: the hero wall-kicks up the closed exit gate (a static collider, so the side is blocked and the kick counts as `wall_jump`, which opens the gate), drifts right past 1344 at y about -100, and Arcade world-bounds collision snaps the body to y 90 in one frame while the camera bounds jump to 0..252. Same when leaving left over x 896. | `src/mechanics/adapters/RoomLockAdapter.ts:155-158`, `src/mechanics/roomLock.ts:73-83`, `src/config/gameplayLayout.ts:4` | Keep the raised ceiling until the hero is below `base.y` or grounded, then restore it. |  |
| MAJOR | principal-engineer | Smoke 49 warps into the channel between the walls (1080, 1100) and out past the gate (1400), so nothing proves a player can get over the left wall, climb the channel, clear the right wall (top y 50) and exit. A shaft that traps the player, or one they can walk past, still passes. The camera's vertical scroll is not asserted (only `cameraHeld` and `room.height`). The 3-attempt retry loop hides flaky kicks. | `scripts/smoke/tutorial-verbs.mjs:106,110-123`, `src/content/campaign.ts:651-652` | Replay from x 900 to past x 1344 with no warp; assert camera scrollY under 0 near the top; one attempt only. |  |
| MINOR | level-designer | shot-wall-kick.png, despite its name, shows Director Iona Vale's line ("Rook's record is clean, WREN...") over a flying enemy, not a tutorial_coach line; the capture no longer demonstrates what its name promises. | `output/smoke-runs/2026-09-24T16-50-54-904Z/49-tutorial-verbs/shot-wall-kick.png` | Rename or re-point that capture so it shows the wall-kick coach text for the next reviewer (prompt 06 handoff row). |  |
| MINOR | level-designer | The coach box still covers roughly the bottom quarter of the frame in shot-armed-charge.png with the armored bot standing just above it (same as the first pass, not new). | `output/smoke-runs/2026-09-24T16-50-54-904Z/49-tutorial-verbs/shot-armed-charge.png` | Deferred with the lane size (06 handoff). |  |
| MINOR | level-designer | The coach box covers roughly the bottom quarter of the playfield in shot-armed-charge.png, where the armored bot and a flying enemy sit near the ground; with the stale text this hides the "charge only" cue while enemies are already live. | `output/smoke-runs/2026-09-24T16-33-00-659Z/49-tutorial-verbs/shot-armed-charge.png` | Shrink or reposition the coach box, or hold enemy spawn until the line clears. |  |
| MINOR | narrative-designer | Line 1's phrasing ("walk before they run") is more idiomatic and soft than the other four, which are flat procedural statements ("That frame is armored", "The wall ahead is scrap"): a small tonal wobble in an otherwise tight set. | `src/content/dialogue/dialogue.v2.json:99` | "Step one. Clear the step ahead. The gate reads your stride, not your file." |  |
| MINOR | narrative-designer | Each line's ordinal ("Step one" to "Step five") is baked into the text but the runtime binding is `lines[index]`; nothing ties the label to the lock it names, so a reordered or inserted lock silently mislabels the prompt. | `src/scenes/game/StoryDirector.ts:136` (`sequence?.lines[index]`), `src/content/dialogue/dialogue.v2.json:99-103` | No text fix now; the handoff must say that reordering hangar locks means editing prose, not only data. |  |
| MINOR | narrative-designer | The bible gives Rook "two lines and a clean record" for the live override scene; the five recorded coach lines add Rook lines across the level. Not a contradiction (the staging note frames them as the pre-recorded clean voice before the held one), but the bible text itself does not carve out the exception. | `docs/story/story-bible.md:49`; `src/content/dialogue/dialogue.v2.json:98` (staging note); `docs/story/script.md:31,50-53` (the live scene still has exactly 2 Rook lines) | Add one clause to the bible's Rook entry: recorded intake prompts during the drill do not count as its live lines. |  |
| MINOR | principal-engineer | If the hero clears the right wall and drifts past x 1344 above y 0 (the gate opens on the first kick), `room` is the locked charge room; the camera snaps from the shaft scroll to 0..252 and the hero is off screen until it falls. The raised-top camera only covers open rooms. | `src/mechanics/adapters/RoomLockAdapter.ts:152-158` | While `top < base.y`, use `Math.min(room.y, top - 8)` as the camera top in the room branch too. |  |
| MINOR | principal-engineer | A swing is counted when it enters `startup`; a swing cancelled in startup by a hit still counts as a scrap-wall hit, and reach is checked at startup rather than when the swing is active. | `src/player/NewPlayerRuntime.ts:168`, `src/mechanics/adapters/RoomLockAdapter.ts:117` | Count on entry to `active`; the counter still catches edges between samples. |  |
| MINOR | principal-engineer | `coachWhileArmed` waits up to 15s without doing the verb, so `phase === 'locked'` is true by construction; the old enqueue-only lane takes about 6.9s per lock and would pass too. `supersede` has no unit test. | `scripts/smoke/tutorial-verbs.mjs:46-47` | Arm lock N while Rook's line N-1 is showing, then assert hint N is showing within 3 frames; unit-test `supersede`. |  |
| MINOR | principal-engineer | The coach line is found by `requiredInput`, not by lock: a sixth lock that repeats a verb replays line one while the validator passes; a seventh lock can never pass both the 4-to-6 line rule and the one-line-per-lock rule. | `src/scenes/game/StoryDirector.ts` (`onRoomLockArmed`, `lines.find`), `src/content/dialogue/validateDialogueContent.ts:258-266` | Pass the lock index to `onArmed` and pick the line by position, which the validator already enforces; derive the line bound from the lock count. |  |
| MINOR | principal-engineer | Still open from the first pass: the `'roomLocks'` string literal. | `src/main.ts:374` | Import `ROOM_LOCK_DATA_KEY`. |  |
| MINOR | principal-engineer | Saber hits are counted by sampling `slashPhase` once per rendered frame for an edge into `active`. At a low frame rate a short active window is missed, and any window wholly inside a physics pause is missed because the adapter returns early while paused; the scrap gate stays short of three hits. | `src/mechanics/roomLock.ts:111`, `src/mechanics/adapters/RoomLockAdapter.ts:92,127` | Expose a `lastSlashStartedAtMs` or a swing counter and detect its change. |  |
| MINOR | principal-engineer | Gameplay reads the runtime's debug surface every frame while a lock is closed: a full debug object allocated per frame, and string keys (`lastDashStartedAtMs`, `lastProjectileSpawnMs`, `lastProjectile.chargeLevel`) that a rename silently breaks. Upward hurt knockback from the ground would count as `jump` if the adapter is reused in 06. | `src/mechanics/adapters/RoomLockAdapter.ts:110-129`, `src/mechanics/roomLock.ts:102` | Add a typed `getVerbSample()` to the runtime; skip the takeoff edge during hurt lock. |  |
| MINOR | principal-engineer | Coach lines are picked by lock index (`lines[index]`) but the validator allows 4 to 6 lines for 5 locks: cutting to 4 silently drops step five; a 6th line never plays. | `src/scenes/game/StoryDirector.ts` (`onRoomLockArmed`), `src/content/dialogue/types.ts:69` | Validate the line count against the tutorial's `roomLocks.length`. |  |
| MINOR | principal-engineer | The payload hardcodes `'roomLocks'` instead of `ROOM_LOCK_DATA_KEY`. | `src/main.ts:374` | Import the constant. |  |

All reviews valid.

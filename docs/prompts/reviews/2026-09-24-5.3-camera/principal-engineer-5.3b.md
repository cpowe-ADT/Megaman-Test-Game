Seat: principal-engineer
Commit: 4eff688
Scope: 05b 5.3b camera fix (EVAL-P5-004), second pass: are the two BLOCKs and the MAJOR from 610fe2c closed, and what regressed
Artifacts opened: output/packets/05b-5.3b-camera-fix-principal-engineer.md, docs/prompts/reviews/2026-09-24-5.3-camera/principal-engineer.md, src/config/hdRenderMath.ts:30-60, src/config/hdRender.ts, node_modules/phaser/src/cameras/2d/BaseCamera.js (getBounds, displayWidth), Camera.js preRender, src/scenes/game/CameraDirector.ts:140-166, src/mechanics/adapters/RoomLockAdapter.ts:135-165, src/scenes/Game.ts, output/smoke-runs/2026-09-24T16-42-26-987Z/18-extended-stage-scroll/state-lookahead.json
Tokens: 64252

Earlier findings: BLOCK 1 (setDeadzone every frame) closed: no `startFollow`, `setDeadzone` or `setFollowOffset` is left; `stopFollow` clears `_follow`, so Phaser's follow code in preRender is skipped; smoke 18 shows scroll going from 504 to 584 inside bounds 1376 wide, the hero at screen x 184.4 and a lead of 39.6 (not measured at scale 2, see the third MAJOR). BLOCK 2 (wrong coordinate space) closed: `displayWidth` is `width/zoomX` (448 at zoom 2) and `getBounds` returns world pixels; the step clamps with `clampScroll`, the same function `HdCamera.clampX` uses (`src/config/hdRender.ts:37-39`). MAJOR (smoke 18 tested nothing) closed: a rigid camera with no look-ahead gives a lead near 0 and fails the [24,48] check; a camera stuck at a bound fails the "strictly inside" check. First-pass MINORs: the mock test and the unreachable vertical mode are closed; `resolveChargeAuraFrequencyMs` moved to `src/player/hitFeel.ts` but the diff adds no test for it.

| Severity | Finding | Evidence | Fix |
| --- | --- | --- | --- |
| MAJOR | When the hero turns while standing behind the anchor, the anchor snaps to the hero in one frame, a jump of up to 64px. Scenario: facing right, knocked back 40px, then presses left: `heroX < anchorX` sets `anchorX = heroX`, so scroll jumps about 45px on that frame. | `src/scenes/game/cameraFollow.ts:112-118` | On a facing flip, move the anchor toward the hero at a capped speed instead of assigning it. Unit test: knockback then turn. |
| MAJOR | Horizontal scroll has no smoothing, so any change of bounds snaps the view in one frame. When a tutorial lock opens, `syncBounds` swaps the room bounds for the stage bounds and scroll jumps from `room.x` to hero x + 40 - 224, about 116px with the hero at room x+300. Leaving the shaft, `restoreCamera` makes the step go from scrolling to locked and `scrollY = bounds.y` snaps (locked to scrolling eases in correctly). The Phaser lerp before 5.3 panned here. | `src/mechanics/adapters/RoomLockAdapter.ts:152-161`; `src/scenes/game/cameraFollow.ts:125-126,130-132` | Keep the previous scroll in the state and ease it toward the clamped target (capped px per ms). Test that the first frame after a bounds change moves less than the cap. |
| MAJOR | The 2x hero-in-frame check has never run: no `camera-2x.json` or `shot-2x-lookahead.png` exists under `output/`; the only run after the fix (16:42Z) has `40-hd-render` failing before the new block. `hdLead >= 24` has no upper bound, so it passes with the camera clamped at the left bound, the same flaw the first pass found in smoke 18. | `output/smoke-runs/2026-09-24T16-42-26-987Z/40-hd-render/pixel-compare.json` (the run fails at the pixel compare, before the new block in `scripts/smoke/hd-render.mjs`); no `camera-2x.json` exists under `output/` | Run `SMOKE_ONLY=40-hd-render` and cite the result. Assert the lead is in [24,48] and scroll strictly inside bounds. |
| MINOR | The follow step runs before the player moves (`tickHitstop` at `src/scenes/Game.ts:1558`, the runtime update at `:1608`, facing at `:1621`), so the camera is one frame behind: about 3.7px at 60fps, 7px at 30fps, and the lag changes with frame time so the hero shimmers when frames are uneven. | `src/scenes/Game.ts:1558,1608,1621` | Step the camera after the runtime update. |
| MINOR | `applyBossRoomCameraLock` clamps with `camera.width` (canvas pixels, 896 at scale 2); for a 448px room this sets scroll one screen too far left. preRender and the next step hide it. | `src/scenes/game/CameraDirector.ts:159-163` | Use `clampScroll(..., displayWidth)`. |
| MINOR | Smoke 18 measures the lead against `replay.finalPlayer.x`, which is stale: `leadLeft` is -44.19 where -40 was expected. | `output/smoke-runs/2026-09-24T16-42-26-987Z/18-extended-stage-scroll/state-lookahead.json` | Use `afterRight.player.x`. |

Answers to the checks: (1) the step gets game pixels and its clamp matches HdCamera's; (2) locking into the boss room snaps as before 5.3, unlocking now snaps too; (3) a respawn far from the anchor snaps, no long pan, and after a respawn behind the hero's facing the anchor sits 64px ahead until the hero passes it; (4) the facing tween is linear and restarts correctly mid-sweep; (5) locked to scrolling is smooth, back snaps; (6) smoke 18 would now fail a broken camera, the hd-render check is unproven; (7) the camera payload in `src/main.ts` is in game pixels and documented in TESTING.md in the same commit. Not checked: no gates run, no screenshots opened.

| Rubric | Score |
| --- | --- |
| Correctness: no path produces a wrong result or crash | 3 |
| Tests: each new branch has a failing-first test or smoke assertion | 3 |
| Contracts: automation hooks, save format and scene flow unchanged or updated with docs | 5 |
| Size: Game.ts shrinks or holds; logic in typed modules | 5 |

Verdict: FIX (both BLOCKs are closed, but the camera now jumps in one frame on turning after knockback and on every lock or unlock, and the 2x check has never run)

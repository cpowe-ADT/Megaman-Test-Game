# Merged review: 2026-09-24-5.3-camera

Seats: game-director (FIX, mean 3.8), principal-engineer (FIX, mean 4.0), principal-engineer (FIX, mean 3.3)

| Severity | Seat | Finding | Evidence | Fix | Agreed |
| --- | --- | --- | --- | --- | --- |
| BLOCK | game-director | The "look-ahead lead at least 24px" assertion is vacuous: `midPointX` is 224 (GAME_WIDTH/2, so `scrollX` clamped to 0) both before and after the 60-frame run, unchanged by 100+px of player motion. The camera never moved; the "lead" of 103.28px is `224 - finalPlayer.x`, a fact about the left world-bound clamp and the player's absolute position near stage start, not about the tweened look-ahead offset, the 0.12/0.08 lerp, or the deadzone. A camera with no look-ahead at all (or a broken one) produces the identical trace as long as the player stays this close to x=0. | `output/smoke-runs/2026-09-24T16-20-04-271Z/18-extended-stage-scroll/state-lookahead.json:4,16` (`midPointX: 224` in both `beforeLookAhead` and `afterLookAhead`) | Run the trace after the hero has passed screen centre and is clear of the left bound (for example `stageDebug.crossNextCheckpoint()` or a seek to a mid-stage x before the 60-frame replay), then assert the offset converges toward the expected deadzone_x/2 plus look-ahead value, not a floor of 24 that a clamp satisfies for free. |  |
| BLOCK | principal-engineer | `tickHitstop` calls `setDeadzone` on every frame. When the camera has a follow target, Phaser's `setDeadzone` moves the camera straight to `scrollX = (x - followOffset.x) - this.width/2`. `this.width` is in canvas pixels, and HdCamera has already added displayWidth/2 into followOffset. So the camera jumps each frame, the lerp and the deadzone never act, and the result is `scrollX = px - lookAhead - 224 - 224*scale`. Failure scenario: render scale 2 (state-0.json `view.scale 2`, `cameraIsHd true`), hero at x 700 facing right. Scroll becomes 68, so the hero draws at screen x 632, outside the 448px view. The bounds clamp still runs after follow, so the boss-room lock holds, but inside a room wider than one screen the hero leaves the visible view. | `src/scenes/game/CameraDirector.ts:188`; Camera.js `setDeadzone` `if (this._follow)` branch; `src/config/hdRender.ts:118-121,179-184` | Call `setDeadzone` only in `startFollowingPlayer` and when the vertical mode changes, or drive scroll from a pure step. |  |
| BLOCK | principal-engineer | The deadzone is also in the wrong coordinate space when set only once. In `preRender`, `fx = px - lookAhead - displayWidth/2` is a scroll value, but the deadzone rectangle is centred on `midPoint`, a world position. The stable band is therefore hero screen x in [416+lookAhead, 480+lookAhead]. Facing left that is [456, 520], so the hero sits past the right edge of the view. | Camera.js `preRender` deadzone branch; `src/config/hdRender.ts:118-121,127` | Move follow into a pure `stepCameraFollow(target, scroll, deadzone, lookAhead, lerp, bounds)` with unit tests, and set scroll from it. The lerp maths itself (`src/player/config.ts:144-150`) is correct at 144Hz and during hit-stop. |  |
| MAJOR | principal-engineer | When the hero turns while standing behind the anchor, the anchor snaps to the hero in one frame, a jump of up to 64px. Scenario: facing right, knocked back 40px, then presses left: `heroX < anchorX` sets `anchorX = heroX`, so scroll jumps about 45px on that frame. | `src/scenes/game/cameraFollow.ts:112-118` | On a facing flip, move the anchor toward the hero at a capped speed instead of assigning it. Unit test: knockback then turn. |  |
| MAJOR | principal-engineer | Horizontal scroll has no smoothing, so any change of bounds snaps the view in one frame. When a tutorial lock opens, `syncBounds` swaps the room bounds for the stage bounds and scroll jumps from `room.x` to hero x + 40 - 224, about 116px with the hero at room x+300. Leaving the shaft, `restoreCamera` makes the step go from scrolling to locked and `scrollY = bounds.y` snaps (locked to scrolling eases in correctly). The Phaser lerp before 5.3 panned here. | `src/mechanics/adapters/RoomLockAdapter.ts:152-161`; `src/scenes/game/cameraFollow.ts:125-126,130-132` | Keep the previous scroll in the state and ease it toward the clamped target (capped px per ms). Test that the first frame after a bounds change moves less than the cap. |  |
| MAJOR | principal-engineer | The 2x hero-in-frame check has never run: no `camera-2x.json` or `shot-2x-lookahead.png` exists under `output/`; the only run after the fix (16:42Z) has `40-hd-render` failing before the new block. `hdLead >= 24` has no upper bound, so it passes with the camera clamped at the left bound, the same flaw the first pass found in smoke 18. | `output/smoke-runs/2026-09-24T16-42-26-987Z/40-hd-render/pixel-compare.json` (the run fails at the pixel compare, before the new block in `scripts/smoke/hd-render.mjs`); no `camera-2x.json` exists under `output/` | Run `SMOKE_ONLY=40-hd-render` and cite the result. Assert the lead is in [24,48] and scroll strictly inside bounds. |  |
| MAJOR | principal-engineer | Both smoke 18 assertions pass without testing anything. midPoint.x is 224 before and after, which means scroll is clamped at 0 on the left stage bound. The hero ends at 120.7, under 224, so the lead of 103 would appear even with look-ahead removed. The "vertical drift" check measures the hero's y, not the camera's. | `scripts/smoke-test.mjs:2873-2883`; `output/smoke-runs/2026-09-24T16-20-04-271Z/18-extended-stage-scroll/state-lookahead.json` | Move the hero at least one screen away from both bounds and let the camera settle. Then assert: scrollX changed and is strictly between the bounds; hero screen x is in [0, 448]; the lead is between 24 and 72 facing right; the sign flips after a left turn. Measure drift on `midPoint.y`. |  |
| MINOR | game-director | The charge-aura reduced-flash period (126ms) is about 7.9Hz, still above the conventional 3-flashes-per-second photosensitivity guidance that "reduced flashing" settings usually target; no capture confirms the on-screen effect under the setting. | `src/player/VfxSfxRouter.ts:173-176` (`CHARGE_AURA_REDUCED_FREQUENCY_MS = 126`) | If the intent is accessibility rather than "less busy", lower the reduced rate to at least 333ms (3Hz at most), or say in the doc comment that this is a feel reduction, not a seizure-safety guarantee, and capture the visual before calling it done. |  |
| MINOR | game-director | Vertical follow mode's "scrolling" branch (`CAMERA_FOLLOW_DEADZONE_Y_SCROLLING = 24`, taller-than-one-screen stages) has a unit test on the pure decision function but no smoke capture, because no such stage exists yet. The "locked" branch (40px) is confirmed on flat ground (0px vertical drift). | `output/smoke-runs/2026-09-24T16-20-04-271Z/18-extended-stage-scroll/state-lookahead.json:19` (`lookAheadVerticalDriftPx: 0`); `src/scenes/game/CameraDirector.ts:27-30` | Ship now since no taller stage exists; flag in the prompt 06 handoff that the scrolling branch needs its own capture once a multi-screen-tall stage lands (5.7's shaft may be that stage). |  |
| MINOR | principal-engineer | The follow step runs before the player moves (`tickHitstop` at `src/scenes/Game.ts:1558`, the runtime update at `:1608`, facing at `:1621`), so the camera is one frame behind: about 3.7px at 60fps, 7px at 30fps, and the lag changes with frame time so the hero shimmers when frames are uneven. | `src/scenes/Game.ts:1558,1608,1621` | Step the camera after the runtime update. |  |
| MINOR | principal-engineer | `applyBossRoomCameraLock` clamps with `camera.width` (canvas pixels, 896 at scale 2); for a 448px room this sets scroll one screen too far left. preRender and the next step hide it. | `src/scenes/game/CameraDirector.ts:159-163` | Use `clampScroll(..., displayWidth)`. |  |
| MINOR | principal-engineer | Smoke 18 measures the lead against `replay.finalPlayer.x`, which is stale: `leadLeft` is -44.19 where -40 was expected. | `output/smoke-runs/2026-09-24T16-42-26-987Z/18-extended-stage-scroll/state-lookahead.json` | Use `afterRight.player.x`. |  |
| MINOR | principal-engineer | The unit-test camera mocks do not copy Phaser's `setDeadzone` side effect (the jump to the target), so 5.3-2 and 5.3-3 pass while the runtime jumps. | `tests/camera-director.test.ts:21-24` | Test the pure step function by the scroll it produces, not by which methods were called. |  |
| MINOR | principal-engineer | The scrolling vertical mode can never be reached. `worldBoundsHeight` is a cached copy that only `applyStageCameraBounds` (always `GAME_HEIGHT`) and the boss lock (built from `GAME_HEIGHT`) write. Any other `setBounds` call bypasses it. Failure scenario: a prompt 06 stage taller than one screen still resolves to `locked`. The "scrolling" deadzone (24) is also narrower than the locked one (40), though the comment says it "opens up". The constant is called a half-extent but `setDeadzone` takes the full width. | `src/scenes/game/CameraDirector.ts:24-25,153-155,163,171`; `src/config/gameplayLayout.ts` (new constants) | Read `camera.getBounds().height` on each tick. Pass the stage height into `applyStageCameraBounds`. Fix the comments. |  |
| MINOR | principal-engineer | `resolveChargeAuraFrequencyMs` has no unit test because `VfxSfxRouter` imports Phaser and AudioService at module load. | `src/player/VfxSfxRouter.ts:1-2,173-177` | Move it to `src/player/hitFeel.ts`, next to `SHAKES` and `resolveShakeRequest`. That file already loads in Node through `src/scenes/game/CameraDirector.ts:13` in the new tests. Add a two-case test. |  |

All reviews valid.

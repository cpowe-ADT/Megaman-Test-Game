Seat: principal-engineer
Commit: 610fe2c
Scope: 05b 5.3 camera (EVAL-P5-004): deadzone, look-ahead tween, split lerp, vertical mode, reducedFlashing, smoke 18; regressions in boss-room lock, bounds, hit-stop
Artifacts opened: output/packets/05b-5.3-camera-principal-engineer.md, src/scenes/game/CameraDirector.ts, src/config/hdRender.ts:24-135, src/config/hdRenderMath.ts:44-55, node_modules/phaser/src/cameras/2d/Camera.js (setDeadzone, preRender; Phaser 3.90.0), src/player/config.ts:138-150, output/smoke-runs/2026-09-24T16-20-04-271Z/18-extended-stage-scroll/state-lookahead.json, state-0.json (camera fields only). shot-0.png confirmed by the orchestrator afterwards: at scale 2 with the hero at x 498 the hero is not in the frame.
Tokens: 51538

| Severity | Finding | Evidence | Fix |
| --- | --- | --- | --- |
| BLOCK | `tickHitstop` calls `setDeadzone` on every frame. When the camera has a follow target, Phaser's `setDeadzone` moves the camera straight to `scrollX = (x - followOffset.x) - this.width/2`. `this.width` is in canvas pixels, and HdCamera has already added displayWidth/2 into followOffset. So the camera jumps each frame, the lerp and the deadzone never act, and the result is `scrollX = px - lookAhead - 224 - 224*scale`. Failure scenario: render scale 2 (state-0.json `view.scale 2`, `cameraIsHd true`), hero at x 700 facing right. Scroll becomes 68, so the hero draws at screen x 632, outside the 448px view. The bounds clamp still runs after follow, so the boss-room lock holds, but inside a room wider than one screen the hero leaves the visible view. | `src/scenes/game/CameraDirector.ts:188`; Camera.js `setDeadzone` `if (this._follow)` branch; `src/config/hdRender.ts:118-121,179-184` | Call `setDeadzone` only in `startFollowingPlayer` and when the vertical mode changes, or drive scroll from a pure step. |
| BLOCK | The deadzone is also in the wrong coordinate space when set only once. In `preRender`, `fx = px - lookAhead - displayWidth/2` is a scroll value, but the deadzone rectangle is centred on `midPoint`, a world position. The stable band is therefore hero screen x in [416+lookAhead, 480+lookAhead]. Facing left that is [456, 520], so the hero sits past the right edge of the view. | Camera.js `preRender` deadzone branch; `src/config/hdRender.ts:118-121,127` | Move follow into a pure `stepCameraFollow(target, scroll, deadzone, lookAhead, lerp, bounds)` with unit tests, and set scroll from it. The lerp maths itself (`src/player/config.ts:144-150`) is correct at 144Hz and during hit-stop. |
| MAJOR | Both smoke 18 assertions pass without testing anything. midPoint.x is 224 before and after, which means scroll is clamped at 0 on the left stage bound. The hero ends at 120.7, under 224, so the lead of 103 would appear even with look-ahead removed. The "vertical drift" check measures the hero's y, not the camera's. | `scripts/smoke-test.mjs:2873-2883`; `output/smoke-runs/2026-09-24T16-20-04-271Z/18-extended-stage-scroll/state-lookahead.json` | Move the hero at least one screen away from both bounds and let the camera settle. Then assert: scrollX changed and is strictly between the bounds; hero screen x is in [0, 448]; the lead is between 24 and 72 facing right; the sign flips after a left turn. Measure drift on `midPoint.y`. |
| MINOR | The unit-test camera mocks do not copy Phaser's `setDeadzone` side effect (the jump to the target), so 5.3-2 and 5.3-3 pass while the runtime jumps. | `tests/camera-director.test.ts:21-24` | Test the pure step function by the scroll it produces, not by which methods were called. |
| MINOR | The scrolling vertical mode can never be reached. `worldBoundsHeight` is a cached copy that only `applyStageCameraBounds` (always `GAME_HEIGHT`) and the boss lock (built from `GAME_HEIGHT`) write. Any other `setBounds` call bypasses it. Failure scenario: a prompt 06 stage taller than one screen still resolves to `locked`. The "scrolling" deadzone (24) is also narrower than the locked one (40), though the comment says it "opens up". The constant is called a half-extent but `setDeadzone` takes the full width. | `src/scenes/game/CameraDirector.ts:24-25,153-155,163,171`; `src/config/gameplayLayout.ts` (new constants) | Read `camera.getBounds().height` on each tick. Pass the stage height into `applyStageCameraBounds`. Fix the comments. |
| MINOR | `resolveChargeAuraFrequencyMs` has no unit test because `VfxSfxRouter` imports Phaser and AudioService at module load. | `src/player/VfxSfxRouter.ts:1-2,173-177` | Move it to `src/player/hitFeel.ts`, next to `SHAKES` and `resolveShakeRequest`. That file already loads in Node through `src/scenes/game/CameraDirector.ts:13` in the new tests. Add a two-case test. |

| Rubric | Score |
| --- | --- |
| Correctness: no path produces a wrong result or crash | 1 |
| Tests: each new branch has a failing-first test or smoke assertion | 2 |
| Contracts: automation hooks, save format and scene flow unchanged or updated with docs | 5 |
| Size: Game.ts shrinks or holds; logic in typed modules | 5 |

Answers to the checks: (1) the look-ahead's `setFollowOffset` works with HdCamera's centring: nothing is doubled; the breakage is the deadzone. (2) The boss-room clamp still holds; the hero leaves the visible view inside the bounds. (3) The time scaling is correct in itself; the look-ahead keeps tweening during hit-stop, harmless because facing is frozen then. (4) See the vertical-mode row. (5) Yes, the lead assertion passes only because the camera is clamped at the left bound. (6) Move it to `src/player/hitFeel.ts`.

Not checked: no gate run. Prediction from `state-0.json` (hero at x 498, scroll clamped to 0 at scale 2): `shot-0.png` should show the hero past the right edge. Confirmed by the orchestrator: the hero is absent from the frame.

Verdict: FIX (the per-frame deadzone jump makes the hero leave the view at render scale 2 or higher, and smoke 18 cannot catch it).

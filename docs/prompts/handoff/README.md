# Handoffs

One file per completed prompt, named `0N-<name>.md`, written only when that prompt's Exit Gate is green. The section list is fixed by `docs/prompts/00-orchestrator-charter.md` section 6. The next prompt reads the previous handoff as its input and refuses to start without it.

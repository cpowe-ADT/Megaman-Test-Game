# Progress

The rolling handoff log for every agent (Claude, Codex, ChatGPT). Read **Now** and the last three entries; nothing older is needed by default. Older entries are verbatim in `docs/archive/progress/`. Write one entry per session with the template below; `npm run agents:check` enforces this file's budget (`tests/agent-budget.json`) and `npm run agents:rotate-progress` moves the oldest entries to the archive when it grows. Live numbers (test counts, `Game.ts` lines, budgets) come from `npm run agents:facts`; do not copy them into prose where they go stale.

## Now

- Branch `codex/05a-feel-hero-camera` (from `main` 93a04a1, PR #58; unpushed). D-015: push and open a PR to main once the prompt 05 Exit Gate is green; never force-push. Prompt 05 is mid-run: 05a and 05b done; 05c in progress: 5.8 playtest fixes PASS (EVAL-P5-011: HUD bars under WebGL, overlays off the floor row, TARGET label), 5.5 hero sheets at STOP 5.5 (EVAL-P5-006, P5-007), then 5.6 profiles (memo `output/notes/05c-5.6.md`, draft module and tests parked in `output/notes/05c-5.6-draft/`), then the 05 Exit Gate.
- Craig's overhaul ask ("better enemies, bosses, graphics, ledges, background, hit boxes, animations") was audited by four seats and folded into 06 and 07 ("Additions from the 2026-09-24 overhaul audit": EVAL-P6-015 hit contract, P6-016 warden sheets, P7-010 boss boxes; findings `docs/prompts/reviews/2026-09-24-overhaul-audit/FINDINGS.md`). 06 starts with the 05b carry-overs listed there.
- Higgsfield: the MCP works (gpt_image_2, reference images by job id). The CLI is 1.1.26 (`higgsfield`, not `hf`: on this Mac `hf` is Hugging Face) and needs Craig's `higgsfield auth login` (approve the page at once; one code expired) and `higgsfield workspace set <id>`.
- Browser gates (full smoke, sweep, footprint) only on a quiet machine: on 2026-09-24 the load average ran 80 to 280 from other sessions, a Docker VM and Safari, and scenarios timed out. Probes use Playwright against the 4180 preview (`output/probes/*.mjs`); the pane pauses Phaser when hidden.
- Craig plays at http://localhost:4180 after `npm run build` (`omega-relay-dist`; 5173 is Docker). `assets/private/` (untracked, 4.5MB) is Craig's to delete; nothing reads it.
- Work lean (`docs/prompts/11-token-efficiency.md`): gates via `npm run -s gate -- ...`, reviews by risk tier with packets, `Tokens:` lines. Craig's to run: `git gc --prune=now`, `npm run clean:artifacts -- --yes` after a dry run.
- Standing gates: `npm run verify` (starts with `agents:check`), `npm run test:visual-sweep`, and after `npm run build`, `npm run perf:footprint`.

## Entry template (1.5KB at most)

```
- YYYY-MM-DD, <prompt part or task>, <agent and model> (e.g. Codex gpt-5, Claude Opus 5.5)
  - Changed: <what, with paths; one line per area>
  - Gates: <command> -> <result line> (<artifact path>)
  - Decisions: <D-ids raised or answered at STOPs; ledger ids moved>
  - Open: <what the next session must know>
  - Tokens: <main session and agent totals from the tool's usage; over a budget in tests/agent-budget.json, say why>
```

## Log (oldest first, newest at the bottom)

- 2026-09-22, prompt 09 parts 09b and 09c and the exit, Claude Code (Opus 5.5)
  - Changed: HUD panels and bars baked into textures (`src/ui/BakedGraphics.ts`, `src/ui/HUD.ts`; a stage frame 4.6ms to 0.4ms headless, HUD pixel-identical); retired enemies destroyed; dead per-frame enemy loop removed; projectile data writes on change; one AudioContext; late music decodes evicted; `VictoryModal` on `GAME_SIZE`; `disk:report` and `clean:artifacts`; `git gc --prune=never`; budgets lowered. Pooling, a save cache, parallax and text-resolution changes measured and dropped (no gain).
  - Gates on `275a3c0`: `npm run verify` exit 0 (`# pass 286`, smoke 51/51), sweep 10/10, `footprint: 22/22 within budget, 0 page errors` (`output/phase-9-exit/`, `output/perf/footprint-09-exit.md`).
  - Decisions: `D-004` to `D-010` raised. Ledger P9-009 to P9-012 PASS. Handoff `docs/prompts/handoff/09-footprint-and-performance.md`.
  - Open: smoke `34-prologue-flow` had a latent 160ms dialogue-debounce race the faster frames exposed; fixed by waiting 12 frames.
- 2026-09-22, prompt 10 part 10a (the agent system), Claude Code (Opus 5.5)
  - Changed: `npm run agents:check` / `agents:context` / `agents:facts` / `agents:reviews` / `agents:rotate-progress` (`scripts/agents/`), `docs/prompts/DECISIONS.md`, `docs/prompts/seats/` and `.claude/agents/game-*.md`, `AGENTS.md` rewritten, `CLAUDE.md`, `START.md` blocks A to G, this log archived and restarted, ledger 01 to 04 archived, charter amendments and rule 15, `docs/prompts/10-agent-system.md`.
  - Gates: same exit run as 09; `tests/agents-checks.test.ts` 16 fixtures; seat review `npm run agents:reviews -- docs/prompts/reviews/2026-09-22-10a --expect docs-steward,qa-eval,principal-engineer` -> `3 reviews, 31 evidenced findings (2 BLOCK), 0 format problems`, all acted on in `275a3c0`.
  - Decisions: `D-001` to `D-003` block entry 5; `D-011`, `D-012` for 10b. Ledger P10-001 to P10-005 PASS. Handoff `docs/prompts/handoff/10a-agent-system.md`.
  - Open: the Codex CLI on this Mac is broken (missing binary), so `scripts/agents/run-seats.sh` has not run; CI does not run `agents:check` yet.
- 2026-09-22, decision panels, 10b and 11a (token efficiency), Claude Code (Opus 5.5)
  - Changed: D-001 to D-012 DECIDED by blind seat panels (`docs/prompts/reviews/2026-09-22-decisions/MERGED.md`, `npm run agents:decisions`); D-001 fixes: radio lane wraps full width, RETRY label below the boss panel, credits footer band (`src/ui/ToastLane.ts`, `src/ui/HUD.ts`, `src/scenes/EndingScene.ts`). 10b: CI runs `agents:check`, provenance in smoke and sweep summaries, `agents:evidence`, `agents:retro`, pack test. 11a: `agents:packet`, `gate`, task and result cards, model and risk tiers, token budgets, `Tokens:` headers and SCORES column (`docs/prompts/11-token-efficiency.md`). Smoke `SMOKE_ONLY` now takes ids and fails when it matches nothing.
  - Gates: `npm run -s gate -- test agents:check` -> `# pass 291`, `0 errors`; `build` PASS; `SMOKE_ONLY=4,14,34,35,36,40` -> `6 ran, 45 skipped` PASS (first try timed out on a cold Vite start, passed on rerun); sweep 10/10; `footprint: 22/22 within budget, 0 page errors` (`output/gates/`). Commits `a4f5386` (UI), `67d2d34` (agents).
  - Decisions: all twelve DECIDED; ledger P10-006 to P10-008 and P11-001 to P11-004, P11-006 PASS; P10-009, P11-005, P11-007, P11-008 PENDING.
  - Open: the first packet-based review (05a) measures the new budgets; `workerStandard` is a guess until then. Close-out: the hand-off rule (`8605b9f`), handoff 10; `npm run -s gate -- verify`: agents:check `0 errors`, test `# pass 291`, build PASS; full smoke did not finish green on this Mac under load (Safari's WebKit GPU process at 100% CPU for two days, Chrome helpers near 70%): `14-completion-return-flow` timed out in verify and `4c-touch-controls` on the rerun, each after 90 to 200s against seconds normally, and both pass alone; the full smoke and sweep run on a clean GitHub runner through CI `browser-gates` (workflow_dispatch); pushed to PR #58: CI `test-and-build` green on push and PR; the first manual `browser-gates` run is red (Linux system fonts in smoke 4, fixed by 08's bundled font; sweep browser close over 5s after artifacts).
  - Tokens: main session about 1.1M before 11a (the audit in prompt 11); 11a's one worker run 57K (haiku, 7 calls). Over budget for a reason: 11a is the change that makes later sessions cheap.
- 2026-09-23/24, prompt 05 part 05a (5.0 harness and health, 5.1 motor truth, 5.2 combat feel), Claude Code (Fable 5.1 orchestrating; workers game-implementer on sonnet and opus; seats qa-eval, release-engineer, game-director, game-code-reviewer)
  - Changed: harness (smoke/sweep continue-on-failure, timestamped run folders with stable symlinks, per-scenario timeout, process-group dev-server stop, stepFrames, loader directory imports, --import loaders, exact pins, engines, requirements.txt, hidden sourcemaps, WebGL page in 40): `scripts/smoke-test.mjs`, `scripts/mission-visual-sweep.mjs`, `src/config/frameStepping.ts`, `tools/`, `package.json`. Extraction: `Game.ts` 3,639 to 2,920 (`src/scenes/game/DevUx.ts`, `DeathSequence.ts`, `CameraDirector.ts`, `RunState.ts`, type-only Phaser, eight seam tests, stageId fix). Motor: no drag, dash-jump carry, 280ms dash with 60ms cooldown from the end, wall-kick grace and buffered wall jumps, jump cut with -400/+250 hero gravity, ledge forgiveness, crouch stop, time-based hit-stop and lerp, 3-frame minimum hold (`src/player/`). Replay: `stageDebug.replayInputs/recordInputs`, 13d from `scripts/smoke/inputs/`. Combat: hit-stop on contact only, hurt lock with blink, charge on press with a frame-time clock, landing squash, death sequence with beam-in, shake cap (`src/player/hitFeel.ts`, `SolidPlatformProbe.ts`).
  - Gates: `npm run -s gate -- agents:check test build` -> PASS (349 tests, Game.ts 2,920); full smoke `output/smoke-runs/2026-09-24T15-07-37-134Z` (51/51 at c9da679); sweep `output/sweep-runs/2026-09-24T15-19-09-811Z` (10/10, hero invulnerable while sampled); `npm run perf:footprint` -> 22/22 within budget (`output/perf/footprint-latest.json`, at 9f3497c).
  - Decisions: D-013 raised (STOP 5.2). Ledger P5-010, P5-001, P5-002, P5-003 PASS.
  - Open: CI browser-gates run after Craig pushes (process-group stop has local evidence only); the in-engine hop-height ladder in 13d (unit numbers 36/36/42/65/142 only); P11-007 skipped (Codex path not run); the review packets' first two seats ran over the 5-call cap before the cap was put on the cards.
  - Tokens: about 1.93M across 21 worker and seat runs (largest: 5.1b replay 234K, 5.2 combat 208K, 5.1c 194K, 5.1 motor 169K, 5.0d 165K); three seats over the 60K seat budget (qa-eval 5.0 66K, code-reviewer 5.0c 67K, qa-eval 5.2 61K) because their packets carried large diffs; workers ran over their call budgets (5.0d 137, 5.1b 133, 5.1c 108, 5.2 72 calls) on real bugs found mid-slice (stepFrames clock, hook race). Main session usage not shown by the tool.
- 2026-09-24, prompt 05 part 05b, phase 5.4 hero design (5.3 and 5.7 in flight), Claude Code (Fable 5.1 orchestrating and doing 5.4 through the Higgsfield MCP; seat art-director)
  - Changed: `docs/art/style-sheet.md` (palette per biome, shipped boss silhouettes, master gpt_image_2 prompt, downscale rule) and `docs/art/hero-brief.md`; turnarounds `assets/sprites/source/player/hero_turnaround_v1_2026-09-24_{a,b,c,c2}.png` with `.prompts.md` (prompts, job ids, URLs); contact sheet `output/art-review/hero-turnarounds.png`.
  - Gates: `npm run -s gate -- agents:check` -> PASS (0 errors, Game.ts 2920) (`output/gates/agents-check.log`). Art Director review `docs/prompts/reviews/2026-09-24-5.4-hero/MERGED.md` FIX: MAJOR on C (ear fins plus face plate read as Mega Man) fixed by excluding C and redrawing C2; MINOR downscale rule added.
  - Decisions: D-014 raised (pick the hero, recommended C2); EVAL-P5-005 PASS at 77a3a23.
  - Open: STOP 5.4 waits on Craig; 5.3 (EVAL-P5-004) and 5.7 (EVAL-P5-009) workers were launched before the STOP and land as their own entries; the outline hue and gradient flattening are checked at the 5.5 cut.
  - Tokens: art-director 35087 (5 calls); one Higgsfield redraw (4 images, about 4 credits); workers reported at landing.
- 2026-09-24, prompt 05 part 05b, phases 5.3 camera and 5.7 the tutorial that teaches (5.4 in the entry above), Claude Code (Fable 5.1 orchestrating; workers game-implementer on sonnet for 5.3 and opus for 5.7, three passes each; seats game-director, game-code-reviewer four times, level-designer twice, narrative-designer, qa-eval)
  - Changed: `src/scenes/game/cameraFollow.ts` (pure follow step) and `CameraDirector` writes scroll in game pixels, `camera` block in render_game_to_text, charge ring under `reducedFlashing` in `hitFeel.ts`; tutorial six screens with `roomLocks`, `wall` faces, `src/mechanics/`, `tutorial_coach` lines and lane hints, smoke 49; smoke 18 and 40 assert the lead mid-stage; smoke 20 waits for the hero to settle.
  - Gates: `npm run -s gate -- agents:check test build` -> PASS (377 tests, Game.ts 2920) (`output/gates/`); full smoke 52/52 at df941b3 (`output/smoke-runs/2026-09-24T17-28-43-801Z`); sweep pass (`output/sweep-runs/2026-09-24T17-33-03-299Z`); footprint 22/22 (`output/perf/footprint-latest.json`).
  - Decisions: D-014 open (hero pick); EVAL-P5-004 and EVAL-P5-009 PASS at df941b3.
  - Open: CI browser gates after Craig pushes; deferred to 06 (tutorial roster, secret, crumble group, lane box over the hero, `shot-wall-kick.png` shows Iona not Rook, coach ordinals live in prose); P11-007 (Codex seat path) still skipped.
  - Tokens: workers over budget: 5.3 114K/222K/315K (79, 48, 45 calls against 40) and 5.7 170K/227K/261K (61, 22, 18 against 70, 50, 30); three code-review seats over 60K (80K, 78K, 64K: diffs of 700+ lines and Phaser internals traced); qa-eval 55K SHIP (`docs/prompts/reviews/2026-09-24-05b-qa/MERGED.md`); the 40-hd-render bisect and the scenario 20 probes were done in-session (about 40 calls) instead of a fourth worker pass, with the two one-line fixes.
- 2026-09-24, prompt 05 part 05c (5.8 playtest fixes, 5.5 hero) and 06 phase 6.P (the tutorial pilot), Claude Code (Claude Opus 5.5 orchestrating; workers game-implementer on sonnet (5.5 pipeline, 6.P engine) and opus (6.P level); seats qa-eval, game-director, art-director, principal-engineer, level-designer, game-code-reviewer)
  - Changed: HUD drawn under WebGL (BakedGraphics), overlays off the floor row, no boss framing before the fight (5.8, 6.P); WREN hero from Higgsfield (96 frames, 45 groups), cutter, coverage and PNG frame audit, developer skin retired (5.5); Drill Hangar tileset, parallax and six enemy families re-skinned, tiles drawn by `src/stage/tileSkin.ts`, tutorial rebuilt without a spike pit, smoke 49 now plays into Rook's fight on real input (6.P); overhaul audit folded into 06 and 07.
  - Gates: `npm run -s gate -- agents:check test build` -> PASS (Game.ts 2920; test pass at f817ff3); `npm run sprites:validate` -> coverage 45 hero groups, audit 96 frames, 36 Python tests OK; focused smoke 49 pass (`output/smoke-runs/2026-09-24T21-56-15-979Z`), 40, 35, 8, 21 pass (engine lane). Full smoke and sweep not run: load average 35 to 280 all afternoon from other sessions, a Docker VM and Safari.
  - Decisions: D-014, D-015, D-016 decided (Craig verbatim); D-017 raised at STOP 6.P. EVAL-P5-011 PASS; P5-006, P5-007 and P6-017..019 wait on the quiet-machine full smoke and sweep and Craig's play.
  - Open: full smoke and sweep; Rook and the eight wardens still the September sheets; lock gates still flat rectangles; the tutorial secret needs a progression change (tutorial has no pickup locations); crumble platforms do not exist; Higgsfield CLI login expired twice (MCP used); 5.6 profiles parked in `output/notes/05c-5.6-draft/`.
  - Tokens: main session large (long run, many captures read). Over budget: 5.5 pipeline worker 147K/75 calls (card 60), 6.P engine worker 225K/204 calls (card 70: a throwaway capture script), 6.P level worker 127K/53; seats 39K to 65K each (principal-engineer 65K over the 60K seat budget on a 1,000-line diff). About 40 Higgsfield generations.

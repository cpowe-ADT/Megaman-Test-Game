# Python environment for scripts/sprites/*.py and tools/sprites/*.py (run through .venv/bin/python
# per AGENTS.md). Pin exact versions so the sprite pipeline is reproducible across machines.
pillow==12.1.1
